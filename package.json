{
  "name": "root",
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:app1:local": "npm run build:local --workspace=app1",
    "build:app1:staging": "npm run build:staging --workspace=app1",
    "build:app1:production": "npm run build:production --workspace=app1",
    "dev:app1": "npm run dev --workspace=app1"
  }
}

{
  "name": "root",
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:app1:local": "cd apps/app1 && npm run build:local",
    "build:app1:staging": "cd apps/app1 && npm run build:staging",
    "build:app1:production": "cd apps/app1 && npm run build:production",
    "dev:app1": "cd apps/app1 && npm run dev"
  }
}
